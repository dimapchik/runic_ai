# Runic AI - Dependencies
# Install: pip install -r requirements.txt
# Tested: Python 3.11+, CUDA 12.4+, PyTorch 2.6+

# ===========================================
# PyTorch 2.6+ (требуется для transformers)
# ===========================================
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# ===========================================
# Data processing
# ===========================================
pandas>=2.0.0
numpy>=1.24.0,<2.1.0          # numpy 2.1+ может ломать совместимость
scikit-learn>=1.3.0           # MAE, RMSE, MAPE метрики для регрессии

# ===========================================
# HuggingFace ecosystem
# ===========================================
transformers>=4.48.0          # Поддержка safetensors и новый API
datasets>=3.0.0
tokenizers>=0.21.0
huggingface-hub>=0.28.0
safetensors>=0.5.0            # Безопасная сериализация моделей

# ===========================================
# Training (жёстко связаны между собой)
# ===========================================
trl>=0.15.0                   # SFTTrainer, DPOTrainer
peft>=0.14.0                  # LoRA адаптеры
accelerate>=1.0.0             # Ускорение обучения

# ===========================================
# Quantization (CUDA 12+)
# ===========================================
bitsandbytes>=0.45.0          # 4/8-bit квантование

# ===========================================
# Metrics
# ===========================================
sacrebleu>=2.5.0              # BLEU, chrF метрики
rouge>=1.0.1                  # ROUGE метрики
evaluate>=0.4.0               # METEOR и другие метрики
nltk>=3.9.0                   # Для METEOR

# ===========================================
# Seq2Seq модели (M2M100, NLLB, MADLAD)
# ===========================================
sentencepiece>=0.2.0          # Токенизатор для M2M100/NLLB
protobuf>=5.0.0               # Для sentencepiece

# ===========================================
# Experiment tracking
# ===========================================
clearml>=1.17.0               # MLOps платформа

# ===========================================
# Visualization
# ===========================================
matplotlib>=3.8.0             # Графики
seaborn>=0.13.0               # Дополнительные визуализации

# ===========================================
# Utilities
# ===========================================
tqdm>=4.66.0                  # Progress bars
